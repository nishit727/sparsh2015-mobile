define([], function () {
	/*
	 * Add data here. The default model defines these attributes for a contact
	 * name: The Night's name
	 * description: like events, supports HTML tags
	 * dates
	 * special
	 */
	var _Nights = [{
		name: '',
		description: '',
		dates: '',
		special: ''
	}];

	return _Nights;
});